#include "../inc/database.hpp"

bool db::connect(class dpp::cluster *bot, const std::string &host,
                 const std::string &user, const std::string &pass,
                 const std::string &db, int port) {
  return true;
};
